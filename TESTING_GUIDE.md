# 瀑布流布局功能测试指南

## 快速开始

### 1. 安装依赖
```bash
cd web
pnpm install
```

### 2. 启动开发服务器
```bash
pnpm dev
```

### 3. 访问应用
打开浏览器访问 `http://localhost:5173`（或终端显示的地址）

## 测试步骤

### 基础功能测试

1. **查看默认布局**
   - 打开首页
   - 确认动态列表以瀑布流形式展示（双列布局）
   - 观察卡片高度自适应内容

2. **切换到列表布局**
   - 点击顶部导航栏右侧的列表图标按钮
   - 确认布局切换为传统的垂直列表
   - 观察切换动画是否流畅

3. **切换回瀑布流布局**
   - 点击顶部导航栏右侧的瀑布流图标按钮
   - 确认布局切换回瀑布流形式
   - 观察切换动画是否流畅

4. **测试状态持久化**
   - 切换到列表布局
   - 刷新页面（F5 或 Cmd+R）
   - 确认页面仍然显示列表布局
   - 切换到瀑布流布局
   - 刷新页面
   - 确认页面显示瀑布流布局

### 响应式测试

1. **桌面端测试**
   - 在正常桌面浏览器窗口查看
   - 确认瀑布流为双列布局
   - 调整窗口大小，观察布局变化

2. **移动端测试**
   - 打开浏览器开发者工具（F12）
   - 切换到移动设备模拟模式
   - 确认瀑布流为单列布局
   - 测试不同设备尺寸

### 功能集成测试

1. **搜索功能**
   - 在搜索框输入关键词
   - 确认搜索结果在当前布局模式下正常显示
   - 切换布局，确认搜索结果保持

2. **标签过滤**
   - 点击某个动态的标签进行过滤
   - 确认过滤结果在当前布局模式下正常显示
   - 切换布局，确认过滤结果保持

3. **加载更多**
   - 滚动到页面底部
   - 点击"继续装填"按钮
   - 确认新加载的内容在当前布局下正常显示

4. **动态操作**
   - 在瀑布流布局下点赞、删除、编辑动态
   - 确认操作正常执行
   - 切换到列表布局
   - 再次测试这些操作

## 预期结果

### 视觉效果
- ✅ 瀑布流布局：卡片以双列（桌面）或单列（移动）形式展示
- ✅ 列表布局：卡片以单列垂直形式展示
- ✅ 切换按钮图标根据当前模式动态变化
- ✅ 布局切换时有平滑的过渡效果

### 功能表现
- ✅ 布局切换即时生效
- ✅ 刷新页面后保持用户选择的布局
- ✅ 所有动态操作在两种布局下都正常工作
- ✅ 搜索和过滤功能不受布局切换影响

### 性能表现
- ✅ 布局切换流畅，无明显卡顿
- ✅ 大量动态加载时性能良好
- ✅ 动画效果不影响交互响应

## 常见问题

### Q: 切换布局后卡片位置混乱？
A: 这是正常的，因为瀑布流和列表的排列逻辑不同。刷新页面即可恢复正常。

### Q: 移动端看不到双列瀑布流？
A: 移动端（屏幕宽度小于 640px）设计为单列，这是预期行为。

### Q: 布局设置没有保存？
A: 检查浏览器是否禁用了 localStorage，或者清除了浏览器缓存。

## 调试技巧

### 查看 localStorage
在浏览器控制台执行：
```javascript
localStorage.getItem('listLayoutMode')
```
应该返回 `"list"` 或 `"waterfall"`

### 手动设置布局
在浏览器控制台执行：
```javascript
localStorage.setItem('listLayoutMode', '"waterfall"')
// 或
localStorage.setItem('listLayoutMode', '"list"')
```
然后刷新页面

### 查看 Pinia Store 状态
在 Vue DevTools 中查看 `echoStore` 的 `listLayoutMode` 值

## 构建生产版本

```bash
cd web
pnpm build
```

构建完成后，静态文件会生成在 `web/dist` 目录。

## 反馈

如果发现任何问题，请记录：
1. 问题描述
2. 复现步骤
3. 浏览器和版本
4. 控制台错误信息（如有）
